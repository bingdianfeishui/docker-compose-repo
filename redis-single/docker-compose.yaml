version: '3.0'
services:
    zoo1:
        image: redis:3.2-alpine
        #restart: always
        container_name: redis_single
        ports:
            - "6379:6379"

networks:
    default:
        external:
            name: mynet
